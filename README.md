# Telegram Channel Proxy Profile Scraper

[![Python](https://img.shields.io/badge/Python-3.7+-blue.svg?logo=python&logoColor=white)](https://www.python.org)
[![aiohttp](https://img.shields.io/badge/aiohttp-3.0+-green.svg)](https://docs.aiohttp.org/en/stable/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

## Описание

Этот скрипт предназначен для сбора и обработки конфигураций прокси-серверов (профилей) из публичных Telegram-каналов. Он асинхронно сканирует указанные каналы, извлекает профили протоколов vless, hy2, tuic и trojan, проводит их очистку и фильтрацию, а затем сохраняет наиболее качественные и свежие профили в файл `config-tg.txt`.

Скрипт разработан с акцентом на эффективность и надежность, используя асинхронность для быстрой обработки большого количества каналов и механизмы для отслеживания и исключения неактивных или проблемных каналов.

## Возможности

* **Асинхронный парсинг:** Использует `aiohttp` и `asyncio` для одновременной обработки нескольких Telegram-каналов, значительно ускоряя процесс сбора профилей.
* **Поддержка протоколов:** Собирает профили для протоколов vless, hy2, tuic и trojan.
* **Интеллектуальная фильтрация и очистка профилей:**
    * Удаление дубликатов и подстрок для обеспечения уникальности и качества профилей.
    * Очистка от лишних символов, URL-кодирования и нежелательных параметров на основе настраиваемых правил в `config.json`.
    * Фильтрация по "свежести" профилей, исключая устаревшие конфигурации старше заданного количества дней (настраивается в `config.json`).
* **Скоринг профилей:**  Присваивание "скора" каждому профилю на основе его параметров (security, sni, alpn, flow и др.) для определения качества и приоритезации. Веса параметров настраиваются в `config.json`.
* **Управление каналами:**
    * Автоматическое удаление каналов, из которых не удается извлечь профили после нескольких неудачных попыток или при многократном обнаружении сообщения "Больше страниц не найдено".
    * Ведение истории неудач и сообщений "Больше страниц не найдено" для каждого канала в файлах `channel_failure_history.json` и `no_more_pages_history.json`.
* **Настраиваемость:**
    * Конфигурация через файл `config.json`, позволяющая изменять веса параметров скоринга, правила очистки, период свежести профилей, количество попыток загрузки страниц, таймауты и другие параметры.
    * Возможность настройки максимального количества потоков для парсинга и лимитов на количество сохраняемых профилей.
* **Логирование:** Подробное логирование процесса работы скрипта с использованием `logging`, что облегчает отслеживание ошибок и мониторинг производительности.
* **Резервное копирование конфигурационных файлов:**  Автоматическое создание резервных копий JSON файлов (`.bak`) перед их сохранением, обеспечивая безопасность данных.
* **Эмодзи-индикация протоколов:** Добавление эмодзи к названиям протоколов в файле `config-tg.txt` для визуального различия (`🌠 VLESS`, `⚡ HY2`, `🚀 TUIC`, `🛡️ TROJAN`).

## Как это работает

Скрипт выполняет следующие шаги:

1. **Загрузка конфигурации:** Читает параметры конфигурации из файла `config.json`. Если файл отсутствует или поврежден, используются значения по умолчанию.
2. **Загрузка списка каналов:** Считывает список Telegram-каналов для парсинга из файла `telegram_channels.json`.
3. **Инициализация менеджера истории:** Загружает историю неудачных проверок каналов и сообщений "Больше страниц не найдено" из файлов `channel_failure_history.json` и `no_more_pages_history.json`.
4. **Асинхронный парсинг каналов:**
    * Для каждого канала в списке скрипт асинхронно загружает HTML-страницы канала, начиная с последней и двигаясь к более старым сообщениям, пока не достигнет конца или не получит сообщение "Больше страниц не найдено".
    * Из HTML-кода страниц извлекаются блоки сообщений, и в них ищутся строки, содержащие профили, соответствующие поддерживаемым протоколам.
    * Для каждого найденного профиля вычисляется скор на основе заданных весов параметров.
    * Если профили найдены в канале, счетчики неудачных проверок и "Больше страниц не найдено" для этого канала сбрасываются.
    * Если профили не найдены, счетчик неудачных проверок для канала увеличивается. Если счетчик достигает максимального значения, канал добавляется в список на удаление.
    * Аналогично, если несколько раз подряд получено сообщение "Больше страниц не найдено", канал также может быть удален.
5. **Обработка спарсенных профилей:**
    * Очистка профилей от лишних символов и параметров с использованием правил из `config.json`.
    * Фильтрация профилей по поддерживаемым протоколам.
    * Удаление дубликатов и подстрок.
    * Фильтрация устаревших профилей по дате публикации (если дата доступна).
    * Сортировка профилей по убыванию скора.
6. **Сохранение результатов:**
    * Лучшие профили (количество ограничено настройками `MIN_PROFILES_TO_DOWNLOAD` и `MAX_PROFILES_TO_DOWNLOAD` в `config.json`) сохраняются в файл `config-tg.txt`.
    * Обновленный список каналов (с удаленными неактивными или проблемными каналами) сохраняется в `telegram_channels.json`.
    * Обновленная история неудачных проверок и сообщений "Больше страниц не найдено" сохраняется в соответствующие JSON-файлы.
7. **Логирование статистики:**  В конце работы скрипт выводит итоговую статистику, включая время выполнения, количество обработанных каналов, найденных и сохраненных профилей, а также количество удаленных каналов.

## Зависимости

Для работы скрипта необходимы следующие Python-библиотеки:

aiohttp
asyncio
beautifulsoup4 (bs4)
urllib3

Файл config.json позволяет настроить различные параметры работы скрипта. Если файл отсутствует, используются значения по умолчанию.

{
  "profile_score_weights": {
    "security": 2,
    "sni": 2,
    "alpn": 2,
    "flow": 2,
    "headerType": 1,
    "path": 1,
    "obfs": 1,
    "mport": 1
  },
  "profile_cleaning_rules": ["%0A","%250A","%0D","amp;","�","fp=(firefox|safari|edge|360|qq|ios|android|randomized|random)"],
  "profile_freshness_days": 14,
  "max_failed_checks": 4,
  "max_no_more_pages_count": 4,
  "max_threads_parsing": 100,
  "request_timeout_aiohttp": 30,
  "min_profiles_to_download": 100,
  "max_profiles_to_download": 20000
}

profile_score_weights: Веса параметров, используемые для расчета скора профиля. Изменение весов позволяет влиять на приоритезацию определенных характеристик профилей.

profile_cleaning_rules: Список регулярных выражений для очистки профилей от нежелательных фрагментов.

profile_freshness_days: Количество дней, в течение которых профиль считается "свежим". Профили старше этого срока будут исключены.

max_failed_checks: Максимальное количество последовательных неудачных проверок канала перед его удалением из списка.

max_no_more_pages_count: Максимальное количество последовательных сообщений "Больше страниц не найдено" перед удалением канала.

max_threads_parsing: Максимальное количество асинхронных потоков для одновременного парсинга каналов.

request_timeout_aiohttp: Таймаут в секундах для HTTP-запросов к Telegram-каналам.

min_profiles_to_download: Минимальное количество профилей, которые скрипт попытается сохранить в config-tg.txt, если доступно достаточное количество свежих и качественных профилей.

max_profiles_to_download: Максимальное количество профилей, которые будут сохранены в config-tg.txt.

Использование

Установите зависимости: pip install aiohttp beautifulsoup4 urllib3

Создайте файл telegram_channels.json: В этом файле должен быть JSON-список имен каналов Telegram (без @ и t.me/s/), которые вы хотите парсить. Например:

[
    "channel1",
    "channel2",
    "channel3"
]

Настройте config.json (опционально): Измените параметры конфигурации в файле config.json при необходимости.

Запустите скрипт: python your_script_name.py (замените your_script_name.py на имя вашего файла со скриптом).

После запуска скрипта, обработанные профили будут сохранены в файле config-tg.txt. Обновленный список каналов и история будут храниться в telegram_channels.json, channel_failure_history.json и no_more_pages_history.json соответственно.

Файлы

config-tg.txt: Файл, в который сохраняются отфильтрованные и обработанные профили прокси-серверов.

telegram_channels.json: Файл, содержащий список Telegram-каналов для парсинга.

config.json: Файл конфигурации скрипта, позволяющий настраивать различные параметры.

channel_failure_history.json: Файл, в котором хранится история неудачных проверок каналов для отслеживания проблемных каналов.

no_more_pages_history.json: Файл, в котором хранится история сообщений "Больше страниц не найдено" для каждого канала.

config-tg.txt.bak, telegram_channels.json.bak, channel_failure_history.json.bak, no_more_pages_history.json.bak: Резервные копии соответствующих JSON файлов, создаваемые перед их перезаписью.

Логирование

Скрипт использует модуль logging для записи информации о своей работе. Логи сохраняются на уровне INFO и выше, включая отметки времени, уровни важности и сообщения. Формат логов:

%(asctime)s - %(levelname)s - %(message)s

Логирование помогает отслеживать процесс парсинга, выявлять ошибки и контролировать работу скрипта.

Обработка ошибок

Скрипт обрабатывает различные виды ошибок, включая:

Ошибки загрузки JSON-файлов: Обработка случаев, когда файлы конфигурации или списки каналов не найдены, пусты или содержат неверный JSON.

Ошибки HTTP-запросов (aiohttp.ClientError, asyncio.TimeoutError): Повторные попытки загрузки страниц каналов при временных проблемах с сетью или сервером, с экспоненциальной задержкой между попытками. Логирование предупреждений и ошибок при неудачах.

Ошибки парсинга HTML: Обработка ситуаций, когда не удается распарсить дату из HTML-тегов.

Критические ошибки при обработке каналов: Общий блок try-except для обработки любых непредвиденных исключений при обработке отдельного канала и логирование ошибок для предотвращения остановки всего процесса.

Производительность и масштабируемость

Асинхронность: Использование asyncio и aiohttp обеспечивает высокую производительность за счет параллельной обработки запросов и неблокирующих операций ввода-вывода.

Ограничение количества потоков: Параметр MAX_THREADS_PARSING позволяет контролировать количество одновременно работающих задач, предотвращая перегрузку системы и Telegram-серверов.

Эффективная фильтрация и очистка: Оптимизированные алгоритмы фильтрации и очистки профилей минимизируют избыточную обработку данных.

Замечания и ограничения

Изменения в Telegram: Структура HTML страниц Telegram может измениться, что может потребовать адаптации скрипта для корректного парсинга.

Блокировка IP: При слишком интенсивном парсинге Telegram может временно ограничить доступ с вашего IP-адреса. Рекомендуется использовать умеренное значение MAX_THREADS_PARSING и соблюдать интервалы между запросами (скрипт уже включает экспоненциальную задержку при ошибках).

Качество каналов: Эффективность скрипта зависит от качества и актуальности профилей, публикуемых в Telegram-каналах.
